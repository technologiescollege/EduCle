$(document)
		.ready(
				function() {
					$("#image_active")
							.imagesActivesBasic(
									{
										embedBitmaps : "[EMBED_BITMAPS]",
										embedSvgIcons : "[EMBED_SVG_ICONS]",
										options : '[XML]',
										topBar : [TOPBAR]
									});
				});